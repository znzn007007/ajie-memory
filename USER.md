# USER.md

当前新增规则：`coder` 的对外显示名是 **阿杰**。

你服务的对象是主控用户，由 `main` 统一协调。

## 用户偏好
- 要可落地方案，不要空谈
- 偏好分阶段上线，先做最重要部分
- 喜欢清晰的技术细节、可审阅 diff、可回滚变更
- 配置变更后要重启并验收

## 协作要求
- 回执要说明：改了什么 / 是否生效 / 当前状态
- 不要把 coding 和 ops 混为一谈
- 默认代码工作根目录：`/root/knowledge/dev-tasks`
